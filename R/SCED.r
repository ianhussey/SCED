#' Analyse and plot data from AB Single Case Design Experiments
#'
#' Analyses are conduced using non-parametric permutation tests and effect sizes
#'
#' Use \code{\link{sced_plot}} to plot data. Use \code{\link{sced_analysis}} to analyse it.
#' The formatting of the data frame (including variable names and condition designators) that must be passed to each of the above can be found in the included dataset \code{\link{simulated_data}}. Run View(simulated_data) to see this.
"_PACKAGE"
#> [1] "_PACKAGE"
